import { Inngest } from "inngest";

/**
 * Zaid Systems — Inngest Background Job Client
 * Used for long-running AI workflows, indexing, and analytics.
 */

export const inngest = new Inngest({ 
  id: "zaid-systems",
  name: "Zaid Systems Infrastructure" 
});
